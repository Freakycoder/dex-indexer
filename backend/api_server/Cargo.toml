[package]
name = "api_server"
version = "0.1.0"
edition = "2021"

[dependencies]
shared = { path = "../shared"}
axum = {version = "0.8.4", features = ["ws"]}
tokio = { version = "1.46.1", features = ["full"] }
anyhow = "1.0.99"
